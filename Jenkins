pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/nandy18/Parking_Pe'
                checkout scm
            }
        }

        stage('Build and Test') {
            steps {
                sh 'npm install'          // Install project dependencies
                sh 'npm run build'        // Build the React project
                sh 'npm test'             // Run tests if applicable
            }
        }

        stage('Deploy') {
            steps {
                // Customize this step to deploy your application to a hosting platform
                // This could involve copying build artifacts to a server or deploying to a service like Netlify, Vercel, or AWS Amplify
            }
        }
    }
}
